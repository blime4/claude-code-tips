---
import Layout from '../layouts/Layout.astro';

const basePath = '/claude-code-tips';
---

<Layout title="快速入门 - Claude Code Tips">
  <article class="container">
    <h1>快速入门</h1>
    <p class="intro">5 分钟上手 Claude Code</p>

    <section>
      <h2>1. 安装</h2>

      <h3>方法一：npm 安装（推荐）</h3>
      <p><strong>中国大陆��户：</strong>使用淘宝镜像加速</p>
      <pre><code># 配置 npm 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 安装 Claude Code
npm install -g @anthropic-ai/claude-code</code></pre>

      <p><strong>其他地区：</strong></p>
      <pre><code>npm install -g @anthropic-ai/claude-code</code></pre>

      <details>
        <summary>权限问题解决方案</summary>
        <p>如果遇到 <code>EACCES</code> 权限错误，可以安装到用户目录：</p>
        <pre><code>mkdir -p ~/.npm-global
npm config set prefix '~/.npm-global'
npm install -g @anthropic-ai/claude-code

# 添加到 PATH（添加到 ~/.bashrc 或 ~/.zshrc）
export PATH=~/.npm-global/bin:$PATH</code></pre>
      </details>

      <h3>方法二：原生安装脚本</h3>
      <pre><code># macOS、Linux、WSL
curl -fsSL https://claude.ai/install.sh | bash

# Windows PowerShell
irm https://claude.ai/install.ps1 | iex</code></pre>

      <h3>方法三：Homebrew（macOS）</h3>
      <pre><code>brew install --cask claude-code</code></pre>
    </section>

    <section>
      <h2>2. 首次运行</h2>
      <pre><code># 进入你的项目目录
cd my-project

# 启动 Claude Code
claude</code></pre>
    </section>

    <section>
      <h2>3. 初始化配置</h2>
      <pre><code># 在 Claude Code 中运行
/init</code></pre>
      <p>这将扫描项目并创建 `CLAUDE.md` 配置文件。</p>
    </section>

    <section>
      <h2>4. 基本命令</h2>
      <table class="commands-table">
        <thead>
          <tr>
            <th>命令</th>
            <th>功能</th>
          </tr>
        </thead>
        <tbody>
          <tr><td><code>/help</code></td><td>显示所有命令</td></tr>
          <tr><td><code>/clear</code></td><td>清空对话</td></tr>
          <tr><td><code>/context</code></td><td>查看 token 使用</td></tr>
          <tr><td><code>/model</code></td><td>切换模型</td></tr>
        </tbody>
      </table>
    </section>

    <section>
      <h2>5. 开始使用</h2>
      <p>现在你可以：</p>
      <ul>
        <li>用自然语言描述你想做什么</li>
        <li>拖拽截图进行分析</li>
        <li>让 Claude 阅读、修改、创建文件</li>
      </ul>
    </section>

    <section class="next-steps">
      <h2>下一步</h2>
      <a href={`${basePath}/tips`} class="btn">浏览所有技巧</a>
    </section>
  </article>
</Layout>

<style>
  article {
    max-width: 800px;
    margin: 0 auto;
    padding: var(--spacing-xl) var(--spacing-lg);
  }

  h1 {
    text-align: center;
    margin-bottom: var(--spacing-sm);
  }

  .intro {
    text-align: center;
    color: var(--color-text-light);
    margin-bottom: var(--spacing-2xl);
  }

  section {
    margin-bottom: var(--spacing-2xl);
  }

  h2 {
    margin-bottom: var(--spacing-md);
  }

  .commands-table {
    width: 100%;
    border-collapse: collapse;
    margin: var(--spacing-lg) 0;
  }

  .commands-table th,
  .commands-table td {
    text-align: left;
    padding: var(--spacing-sm) var(--spacing-md);
    border-bottom: 1px solid var(--color-border);
  }

  .commands-table th {
    font-weight: 600;
    color: var(--color-text);
  }

  .commands-table td {
    color: var(--color-text-light);
  }

  ul {
    margin-left: var(--spacing-lg);
  }

  .next-steps {
    text-align: center;
    padding: var(--spacing-2xl) 0;
  }
</style>
