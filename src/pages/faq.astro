---
import Layout from '../layouts/Layout.astro';
---

<Layout title="常见问题 - Claude Code Tips">
  <article class="container">
    <h1>常见问题</h1>
    <p class="subtitle">Claude Code 使用中的常见问题解答</p>

    <section class="faq">
      <details class="faq-item">
        <summary>Claude Code 支持哪些编程语言？</summary>
        <div class="faq-content">
          <p>Claude Code 支持几乎所有编程语言，包括但不限于：</p>
          <ul>
            <li><strong>Web 前端</strong>: JavaScript, TypeScript, React, Vue, Astro</li>
            <li><strong>后端</strong>: Python, Node.js, Go, Java, Rust</li>
            <li><strong>移动端</strong>: Swift, Kotlin, React Native, Flutter</li>
            <li><strong>其他</strong>: SQL, Shell, Docker, Kubernetes</li>
          </ul>
        </div>
      </details>

      <details class="faq-item">
        <summary>我的代码会被发送到哪里？</summary>
        <div class="faq-content">
          <p>代码会被发送到 Anthropic 的 API 进行处理。Anthropic 会：</p>
          <ul>
            <li>不使用你的代码训练模型</li>
            <li>在处理后立即删除</li>
            <li>遵守严格的隐私政策</li>
          </ul>
          <p>详见 <a href="https://www.anthropic.com/legal/privacy" target="_blank">Anthropic 隐私政策</a>。</p>
        </div>
      </details>

      <details class="faq-item">
        <summary>如何限制 Claude Code 可以访问的文件？</summary>
        <div class="faq-content">
          <p>你可以通过以下方式限制访问：</p>
          <ul>
            <li>使用 <code>.gitignore</code> 规则</li>
            <li>在 <code>CLAUDE.md</code> 中明确指定允许的路径</li>
            <li>使用权限白名单配置</li>
          </ul>
          <pre><code># 在 CLAUDE.md 中
## 允许的路径
- src/
- tests/
- docs/

## 禁止访问
- .env
- *.secret
- node_modules/</code></pre>
        </div>
      </details>

      <details class="faq-item">
        <summary>Claude Code 会自动修改我的代码吗？</summary>
        <div class="faq-content">
          <p>默认情况下，Claude Code 会<strong>询问确认</strong>后再修改。如果你想让它直接执行：</p>
          <ul>
            <li>按 <code>Shift+Tab</code> 切换到 Command 模式</li>
            <li>或在配置中启用 <code>dangerouslySkipPermissions</code></li>
          </ul>
          <p class="warning">⚠️ 跳过确认仅在可丢弃的环境中使用。</p>
        </div>
      </details>

      <details class="faq-item">
        <summary>token 使用超限了怎么办？</summary>
        <div class="faq-content">
          <p>使用 <code>/context</code> 查看使用情况。如果接近限制：</p>
          <ul>
            <li>让自动压缩处理（推荐）</li>
            <li>或使用 <code>/clear</code> 开始新对话</li>
            <li>或启动新会话实例</li>
          </ul>
        </div>
      </details>

      <details class="faq-item">
        <summary>如何让 Claude Code 遵循团队的代码风格？</summary>
        <div class="faq-content">
          <p>在 <code>CLAUDE.md</code> 中定义你的代码规范：</p>
          <pre><code>## 代码风格
- 使用 2 空格缩进
- 单引号优先
- 组件不超过 200 行
- 函数必须有类型注解</code></pre>
          <p>或使用 hooks 自动格式化：</p>
          <pre is:raw><code>// .claude/hooks.json
{
  "hooks": {
    "PostToolUse": {
      "Edit": "prettier --write $FILE"
    }
  }
}
</code></pre>
        </div>
      </details>

      <details class="faq-item">
        <summary>可以在 CI/CD 中使用 Claude Code 吗？</summary>
        <div class="faq-content">
          <p>可以！常见用途包括：</p>
          <ul>
            <li>自动代码审查</li>
            <li>生成文档</li>
            <li>运行测试并分析结果</li>
            <li>自动创建 PR 描述</li>
          </ul>
          <p>记得配置 CI 环境变量并提供 API Key。</p>
        </div>
      </details>

      <details class="faq-item">
        <summary>Claude Code 和 GitHub Copilot 有什么区别？</summary>
        <div class="faq-content">
          <table>
            <thead>
              <tr>
                <th></th>
                <th>Claude Code</th>
                <th>GitHub Copilot</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td>主要形式</td>
                <td>对话式 AI 助手</td>
                <td>代码补全</td>
              </tr>
              <tr>
                <td>使用方式</td>
                <td>自然语言指令</td>
                <td>IDE 内实时补全</td>
              </tr>
              <tr>
                <td>能力范围</td>
                <td>读写文件、运行命令</td>
                <td>代码生成建议</td>
              </tr>
              <tr>
                <td>适合场景</td>
                <td>复杂任务、重构、调试</td>
                <td>日常编码提速</td>
              </tr>
            </tbody>
          </table>
          <p>两者可以配合使用获得最佳效果！</p>
        </div>
      </details>
    </section>

    <section class="related">
      <h2>还有问题？</h2>
      <p>查看 <a href="/tips">所有技巧</a> 或查阅 <a href="https://docs.anthropic.com" target="_blank">官方文档</a>。</p>
    </section>
  </article>
</Layout>

<style>
  article {
    max-width: 800px;
    margin: 0 auto;
    padding: var(--spacing-xl) var(--spacing-lg);
  }

  h1 {
    text-align: center;
    margin-bottom: var(--spacing-sm);
  }

  .subtitle {
    text-align: center;
    color: var(--color-text-light);
    margin-bottom: var(--spacing-3xl);
  }

  .faq {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }

  .faq-item {
    background: var(--color-surface);
    border-radius: var(--radius-lg);
    border: 1px solid var(--color-border);
    overflow: hidden;
  }

  .faq-item summary {
    padding: var(--spacing-lg);
    cursor: pointer;
    font-weight: 500;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .faq-item summary::-webkit-details-marker {
    display: none;
  }

  .faq-item summary::after {
    content: '+';
    font-size: 1.5rem;
    font-weight: 300;
  }

  .faq-item[open] summary::after {
    content: '−';
  }

  .faq-item summary:hover {
    background: var(--color-bg);
  }

  .faq-content {
    padding: 0 var(--spacing-lg) var(--spacing-lg);
    border-top: 1px solid var(--color-border);
  }

  .faq-content p {
    margin-bottom: var(--spacing-md);
  }

  .faq-content ul {
    margin-left: var(--spacing-lg);
    margin-bottom: var(--spacing-md);
  }

  .faq-content code {
    background: var(--color-bg);
    padding: 0.15em 0.4em;
    border-radius: var(--radius-sm);
  }

  .faq-content pre {
    background: var(--color-code-bg);
    color: #E8E8E8;
    padding: var(--spacing-md);
    border-radius: var(--radius-md);
    overflow-x: auto;
  }

  .faq-content table {
    width: 100%;
    border-collapse: collapse;
    margin: var(--spacing-md) 0;
  }

  .faq-content th,
  .faq-content td {
    padding: var(--spacing-sm);
    border: 1px solid var(--color-border);
    text-align: left;
  }

  .faq-content th {
    background: var(--color-bg);
    font-weight: 600;
  }

  .warning {
    background: #FFF8E1;
    color: #F57F17;
    padding: var(--spacing-sm) var(--spacing-md);
    border-radius: var(--radius-sm);
    display: inline-block;
  }

  .related {
    text-align: center;
    margin-top: var(--spacing-3xl);
    padding-top: var(--spacing-xl);
    border-top: 1px solid var(--color-border);
  }
</style>
